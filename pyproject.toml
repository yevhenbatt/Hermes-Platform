[project]
name = "hermes-platform"
version = "0.1.0"
description = "Hermes Platform extensions for Hermes Agent"
requires-python = ">=3.12"

dependencies = []

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["hermes_platform"]
